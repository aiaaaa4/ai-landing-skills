#!/usr/bin/env python3
from __future__ import annotations


QUESTIONNAIRE = """开始视频封装前，请一次性确认以下内容：
1. 免责声明：默认在开头添加固定免责声明并显示 3 秒；如不需要，请回复“关闭”。
2. 封面候选：默认从视频前半段抽取 5 张独立 PNG；如不需要，请回复“关闭”。
3. 发布版字幕：项目内存在匹配的双语 SRT 时，默认生成时间轴校准后的发布版双语 SRT，不烧录进视频。
4. 高级处理：默认不加水印、不烧录字幕、不裁切、不加滤镜，也不重新编码视频主体；如需要，请逐项填写。
5. 输出位置：默认保存在原视频项目文件夹，并命名为“原视频名.发布版.mp4”。
6. 文件覆盖：默认不覆盖任何已存在的同名文件；如允许覆盖，请明确说明。

如全部使用默认设置，请回复：确认默认设置。"""


def main() -> int:
    print(QUESTIONNAIRE)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
