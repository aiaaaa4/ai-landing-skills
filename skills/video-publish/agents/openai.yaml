interface:
  display_name: "极简视频封装"
  short_description: "抽取 5 张投稿封面，轻量添加免责声明，并按实际语音偏移同步字幕。"
  default_prompt: "使用 $video-publish 时原样发送 scripts/preflight.py 的固定问卷；组合流程复用已确认答案。默认抽取 5 张封面、添加 3 秒免责声明并校准发布版 SRT；长任务保持前台或轮询同一 session，禁止结束轮次等待通知。"
policy:
  allow_implicit_invocation: false
